{
	"name": "@nvisy/graph",
	"version": "0.1.0",
	"description": "Workflow graph engine for defining and executing processing pipelines",
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsup",
		"build:watch": "tsup --watch",
		"clean": "rimraf dist",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@nvisy/connect": "*",
		"@nvisy/core": "*",
		"@nvisy/process": "*"
	},
	"engines": {
		"node": ">=22.0.0"
	}
}
