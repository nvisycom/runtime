{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		/* Emit */
		"outDir": "./dist",
		"rootDir": "./src",
		/*
		 * Override: The root tsconfig uses customConditions: ["source"] to
		 * resolve workspace packages from .ts source. However, some
		 * third-party deps (eventsource-parser) also export a "source"
		 * condition pointing at raw .ts files that fail to compile.
		 * Removing the condition here forces resolution through compiled
		 * outputs for all deps while workspace packages still resolve
		 * via their "source" condition in the root.
		 */
		"customConditions": []
	},
	/* Scope */
	"include": ["src/**/*"],
	"exclude": ["node_modules", "dist"]
}
