{
	"name": "@nvisy/runtime",
	"version": "0.1.0",
	"description": "Graph definition, DAG compilation, execution engine, and scheduler for the Nvisy platform",
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsup",
		"build:watch": "tsup --watch",
		"clean": "rimraf dist",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@nvisy/core": "*",
		"effect": "^3.16.0"
	},
	"engines": {
		"node": ">=22.0.0"
	}
}
